function chessitems(x, y, name, type) {

let self = {x, y, name, type};

    function  init () {
        
    }init( );
    return self;
}

function xe(x, y, type = 0) {

    let self = chessitems(x, y, 'xe', type);

    function  init () {
        
    }init( );
    return self;
    
}

function phao(x, y, type = 0) {

    let self = chessitems(x, y, 'phao', type);

    function  init () {
        
    }init( );
    return self;
    
}

function ma(x, y, type = 0) {

    let self = chessitems(x, y, 'ma', type);

    function  init () {
        
    }init( );
    return self;
    
}

function tuongs(x, y, type = 0) {

    let self = chessitems(x, y, 'tuongs', type);

    function  init () {
        
    }init( );
    return self;
    
}

function sy(x, y, type = 0) {

    let self = chessitems(x, y, 'sy', type);

    function  init () {
        
    }init( );
    return self;
    
}

function tuongj(x, y, type = 0) {

    let self = chessitems(x, y, 'tuongj', type);

    function  init () {
        
    }init( );
    return self;
    
}

function tot(x, y, type = 0) {

    let self = chessitems(x, y, 'tot', type);

    function  init () {
        
    }init( );
    return self;
    
}